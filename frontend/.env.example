# Frontend environment variables example
# Copy to .env.production for production builds
# Copy to .env.development for local development

# API base URL for the backend
# For Docker Compose (Nginx proxies /api/ to backend): /api/v1
# For separate deployments: https://your-backend-domain.com/api/v1
VITE_API_BASE_URL=/api/v1
